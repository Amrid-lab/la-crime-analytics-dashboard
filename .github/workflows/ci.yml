name: CI
on: [push, pull_request]
jobs:
  syntax:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: r-lib/actions/setup-r@v2
      - name: Parse R sources
        run: Rscript -e 'for (f in list.files(pattern="[.][Rr]$")) parse(file=f, encoding="UTF-8")'
